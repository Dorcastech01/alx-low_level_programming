#include "main.h"

/**
 * _isupper - main block
 * @c: user input
 * Description: script to print uppercase
 * Return: 0
 */
int _isupper(int c)
{
	if (c >= 65 || c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
