#include "main.h"
/**
 * mul - main block
 * @a: user first input
 * @b: user second input
 * Description: script to multiply two numbers
 * Return: result
 */
int mul(int a, int b)
{
	return (a * b);
}
