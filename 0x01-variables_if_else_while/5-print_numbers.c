#include <stdio.h>
/**
 * main - main block
 * Description: script to print numbers
 * Return: 0
 */
int main(void)
{
	int a = 0;

	for (a = 0; a <= 9 ; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
