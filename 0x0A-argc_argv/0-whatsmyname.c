#include <stdio.h>
/**
 * main - main function
 * @argc: number of CLA
 * @argv: array of strings
 * Description: script that prints program name
 * Return: result
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}

